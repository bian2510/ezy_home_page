task_id: '015'
title: 'Docker multi-stage build + nginx SPA routing'
assigned_agent: devops-engineer
status: completed
requires_reading:
  - .etc_sdlc/features/active/F001-ezyhome-storefront-page/spec.md
  - .etc_sdlc/features/active/F001-ezyhome-storefront-page/design.md
files_in_scope:
  - Dockerfile
  - docker-compose.yml
  - nginx.conf
  - .env.example
acceptance_criteria:
  - 'Dockerfile multi-stage: Stage 1 node:20-alpine enables pnpm via corepack enable, copies package files, runs pnpm install --frozen-lockfile, copies src, runs pnpm build. Stage 2 nginx:stable-alpine copies dist from Stage 1 and nginx.conf to default.conf. Runs as non-root user.'
  - 'nginx.conf: try_files $uri /index.html for SPA routing; Cache-Control immutable max-age 31536000 for hashed assets; Cache-Control no-cache for index.html.'
  - 'docker-compose.yml maps port 3000 to container 80, passes VITE_WHATSAPP_NUMBER as build arg. .env.example documents VITE_WHATSAPP_NUMBER with instructions digits only no + prefix.'
dependencies:
  - '014'
